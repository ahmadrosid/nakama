name: Quality

on:
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Scoped to apps/ and packages/ so generated output that is not in git
      # (graphify-out/, ownership-map-out/) cannot fail a PR.
      - name: Lint
        run: bun x ultracite check apps packages

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # apps/server builds with `bun build`, which does not type check, so
      # without this job a type error in server, core, agent or db cannot reach
      # CI. The web app is the one package whose build already ran tsc.
      - name: Type check web
        run: bun run --cwd apps/web build

  duplication:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      # Fails when duplication passes the threshold in .jscpd.json (3%).
      - name: Detect copy-paste
        run: bun run dupes

      - name: Publish report
        if: always()
        run: cat .jscpd-report/jscpd-report.md >> "$GITHUB_STEP_SUMMARY"

  coverage:
    runs-on: ubuntu-latest
    # A report, not a gate. Unit Tests is what blocks a merge.
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests with coverage
        run: bun test --coverage --coverage-reporter=text | tee coverage.txt

      - name: Publish summary
        if: always()
        run: |
          table() {
            awk '/^File .*% Funcs/{p=1} /^ *[0-9]+ (pass|fail)/{p=0} p' coverage.txt
          }
          {
            echo '## Coverage'
            echo '```'
            table | head -3
            echo '```'
            echo '<details><summary>Per-file</summary>'
            echo
            echo '```'
            table
            echo '```'
            echo '</details>'
          } >> "$GITHUB_STEP_SUMMARY"
