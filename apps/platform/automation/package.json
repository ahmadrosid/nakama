{
  "name": "@nakama/automation",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production bun run src/index.ts",
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir dist --target bun --external pdf-parse",
    "test": "bun test"
  },
  "dependencies": {
    "@nakama/client": "workspace:*",
    "@nakama/core": "workspace:*"
  }
}
