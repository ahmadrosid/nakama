{
  "name": "@nakama/telegram",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production bun run src/index.ts",
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir dist --target bun --external @vscode/ripgrep --external pdf-parse",
    "test": "bun test"
  },
  "dependencies": {
    "@nakama/client": "workspace:*",
    "@nakama/core": "workspace:*",
    "grammy": "^1.35.0"
  }
}
